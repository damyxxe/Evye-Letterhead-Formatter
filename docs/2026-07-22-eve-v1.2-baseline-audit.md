# Eve v1.2.0 Baseline Audit — 2026-07-22

Close-off audit for the v1.2.0 formatting-parity release. Verified live, not from memory.

## Live state (verified)

- **evye-docgen** (`co.evye.docgen`, Alfred, port 8091): `GET /docs/api/health` →
  `{"status":"ok","version":"1.2.0"}`.
- **Evye Assistant** n8n workflow (`KmAFj9dA7wCFGxnQ`, `https://webhooks.evye.co`): 40
  nodes, `active: true`, zero occurrences of the old hardcoded token strings in the live
  workflow JSON (checked via API export).
- **n8n container env**: `NOTION_TOKEN_EVE` and `TELEGRAM_TOKEN_EVE` both present
  (`docker exec n8n printenv | grep -c` → `2`).
- **Repo**: `origin/main == local main == HEAD` at merge commit `6fda219` (PR #1, merged
  2026-07-22). 36/36 pytest tests green on `main` after merge.

## Categorisation

- **Core/active**: `word_generator.py`, `api.py`, `templates/evye-master.docx`, `tests/`,
  the `Evye Assistant` n8n workflow.
- **Kept-inactive**: n/a for this project.
- **Removed as dead/cruft** (this release): `fix_hierarchy_and_bullets.py`,
  `fix_indentation_and_styles.py`, `fix_page2_consistency.py`,
  `fix_resilience_and_mapping.py`, `fix_sample_text.py`, `fix_table_styling.py`,
  `generator.py`, `templates/{base,contract,letter,quotation}.html` (WeasyPrint/PDF-era,
  git history preserves them); n8n nodes `Extract Request`, `GET Notion Blocks`,
  `Extract Table IDs`, `Has Tables?`, `GET Table Rows`, `Rebuild Blocks`.
- **Deliberately out of scope, left alone**: `build_master_template.py`,
  `example_contract.py`, `example_quotation.py`, `update_master_styles.py` — one-off
  build/example scripts at repo root, not in the plan's delete list; not re-litigated here.
- **Convention gaps**: none found — `.gitignore` already covers `.DS_Store`, `__pycache__`,
  `*.pyc`, `templates/*.bak`; no untracked cruft in the working tree at close-off time.

## Drift resolved

Production (`alfred:/opt/evye-docgen/word_generator.py`) had diverged from git before this
release (recursive block-children fetching + toggle support existed live but not in the
repo). Task 1 of the implementation plan synced deployed → git first, tagged `eve-v1.1.0` as
the pre-v1.2 baseline, before any further edits. Deployed and git are now identical as of
`eve-v1.2.0`.

## Baseline tags

- `eve-v1.1.0` — synced-from-Alfred baseline, pre-v1.2 work.
- `eve-v1.2.0` — this release.

No separate `-baseline-` tag added; this project's own plan already establishes
`eve-v1.1.0`/`eve-v1.2.0` as the versioning convention, so it's used as-is rather than
introducing a second naming scheme.

## Pending / next version

See CHANGELOG.md "Pending" section — secret rotation (deferred by Michael 2026-07-22, not
resolved), live Telegram UAT, and the v1.3 scope list (table-cell run formatting, Notion
color annotations, callout icon/tint, LLM-based intent extraction).
